@inject IdentityService IdentityServices
<style>    
.btn-floating {
        position: fixed;
        bottom: 20px;
        right: 20px;
        z-index: 1000; /* Ensures the button stays on top of other content */
    }
</style>
<a asp-action="AquisitionForm" asp-controller="Home" class="btn btn-lg btn-warning text-white text-center rounded-circle btn-floating">+</a>
<component type="typeof(Acquisitions)" render-mode="ServerPrerendered" param-CompanyName= "@IdentityServices.GetCompanyNameAsync(User.Identity!.Name!)" />
